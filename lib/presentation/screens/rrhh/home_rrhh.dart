import 'package:flutter/material.dart';

class HomeRrhh extends StatelessWidget {

  static const String name = 'home_rrhh';

  const HomeRrhh({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text("RR.HH"),
      ),
    );
  }
}